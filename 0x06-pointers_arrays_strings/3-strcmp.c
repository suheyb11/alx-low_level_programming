#include "main.h"
#include <string.h>

/**
 * _strcmp - > is funtion that comp
 * @s1: is one prm
 * @s2: is seconf prm
 * Return: is voud not do
 */
int _strcmp(char *s1, char *s2)
{
	strncmp(s1, s2):
	return (s1);
}
