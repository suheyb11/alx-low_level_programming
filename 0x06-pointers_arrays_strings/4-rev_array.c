#include "main.h"

/**
 * reverse_array - > reverse_array is funtion
 * @a: is one ormn
 * @n: teo prm
 * Return: is void not crct
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = n; i < n - 1; i--)
	{
		return (a[i]);
	}
}
